# Submission checklist — internal target 26 August 2026

**Official deadline:** 30 August 2026, 23:59 GST (UTC+4) = 19:59 UTC = 21:59
Europe/Berlin = **12:59 America/Phoenix on 30 August**. The rules say *"no late
submissions."* The owner-set working deadline is **26 August 2026**.

Boxes are checked only where a command, public response or retained artefact was
verified. External actions remain unchecked until the responsible person or API
confirms them.
Requirement-by-requirement status lives in
[`hackathon-compliance.md` §6](hackathon-compliance.md); this file is the ordered
list of things a person still has to do.

## Verified release update — 26 August 2026

- Clean publication root: `16e26afdf1e33dad872c6e55077887084f3b718f`;
  one root, one commit at publication, and no inherited pull-request refs.
- Public head: `8fdeed80b27e58dd39f5c2eec3e478ec6d95bb27`; GitHub CI
  [run 32912448603](https://github.com/Damso74/fortyguard-heat-priority-engine/actions/runs/32912448603) succeeded on that exact commit.
- Vercel: [public production](https://heat-priority-engine.vercel.app), deployment
  `dpl_CCcbvpQSpdrAQEN44FcAcH19kR8p`, `READY`, deployed from the clean public
  checkout on 2026-08-26.
- Logged-out production smoke: all nine product routes plus the OG image returned
  HTTP 200; desktop and 412×915 checks found no browser error or horizontal
  overflow.
- Production safety endpoint: `/api/fortyguard/status` returned HTTP 200 with
  `configured=false`, `liveEnabled=false`, `authHeaderName=api-key`.
- Video: the [final non-listed jury video](https://youtu.be/LPq0Tn6YX9w) is
  1:25, uses the current release and has active English captions.
- Official sources re-read: [hackathon page](https://www.fortyguard.com/hackathon26)
  and [current submission form](https://docs.google.com/forms/d/e/1FAIpQLSdheKfejq4uAk5dNluoaH6yBAL9N78-E1H8c_8FSnSMZKGlqQ/viewform).

Two kinds of item, kept apart on purpose:

- **Local** — doable in this working copy, reversible, needs nobody's permission.
- **External** — leaves the machine or depends on another party. None of these
  can be completed by editing this repository, and none may be reported as done
  until the person who did it says so.

---

## 0. Raw-file publication gate

- [x] **External · decision.** Exact-item terms reviewed 2026-08-22 settle two
      of four files. Obtain direct permission for the City BusStops and quarterly-
      ridership extracts, or remove those two. The owner selected Path B on
      2026-08-23.
  - Path A — establish direct permission: record its scope, link and date.
    Requests were sent on 2026-08-23 from `dcredoz@leadalpes.fr` to the City of
    Phoenix and Valley Metro; replies are pending. The exact scope and evidence
    checklist are in
    [`licensing-permission-request.md`](licensing-permission-request.md).
  - Path B — completed: the original repository is a private archive, and the
    public submission repository starts from the verified clean snapshot with no
    inherited PR refs or historical objects. This also gives an unambiguous
    post-kickoff repository creation date.
  - The permission requests also cover public redistribution of processed
    per-stop fields. Record any reply before calling the broader licensing
    question resolved.
- [x] **Local.** The two established Valley Metro item grants are recorded in
      `data-provenance.md` §8, the source manifest and fetch metadata.
- [x] **Local.** `data-provenance.md` §8,
      `hackathon-compliance.md` §5, the README licensing block and the demo
      script's closing line so all say the same thing.

The raw-file gate is resolved. Pending direct-permission replies remain recorded
as due diligence for the processed fields and must be incorporated if received.

---

## 1. Local — verification and truthfulness

- [x] `npm run verify` green locally on the verified product state on
      2026-08-26; current counts are owned by `verification-report.md`. Nothing
      skipped, nothing softened.
- [x] `docs/verification-report.md` regenerated by that run. Commit it with
      the counts it actually produced.
- [x] `npm run scan:secrets` clean; only `.env.example` is tracked. The local
      `.env.local` remains ignored. On the final repository, confirm `git ls-files
      '.env*'` returns
      nothing but `.env.example`.
- [x] `npm run check:snapshots` green — every committed snapshot is a valid real
      capture, no two answer the same request.
- [x] Re-read [`findings-provenance.md`](findings-provenance.md) against the
      README, the submission draft and the demo script. Any figure quoted in more
      than one place either matches or points to the one file that owns it.
- [x] No document quotes a test count in prose. The generated report owns those.
- [x] Confirm the pilot scope wording is identical everywhere: 27 Downtown stops,
      450 cells, three completed activities, `EXPOSURE_ONLY`, 3 robust + 7
      conditional at capacity 10. No Central Phoenix, no Full Phoenix, no
      city-wide claim.

## 2. Local — the product a judge will open

- [x] Production build, walked end to end: overview, heat monitor, planner,
      missions, review, scenario lab, reports, methodology. No dead route.
- [x] The banner reads `CACHED REAL DATA` on the default run.
- [x] A mission was submitted, reviewed and accepted in a logged-out production
      session, producing plan version 2.
- [x] An export downloaded a non-empty CSV **and** JSON, in the same session as
      the run that produced it.
- [x] The methodology page renders the claim registry, including the blocked
      claims.
- [x] No API key, seal, flag, agency logo or endorsement anywhere on screen;
      local E2E and secret-scan checks are green.

## 3. Local — video review and approval

- [x] Capture the deployed production build against [`demo-script.md`](demo-script.md).
- [x] Record a jury-focused local candidate with the owner's cloned voice:
      `outputs/submission-video/fortyguard-demo-jury-damien.mp4`, H.264/AAC,
      1440×860, 2:32.88, normalized audio, synchronized navigation and guided
      cursor. The report screen still exposes the licensing `Blocked` state, so
      this remains a review candidate rather than an uploadable final.
- [x] Inspect representative frames and keep the recording ignored by Git.
- [x] Prepare and verify the accessible assembly pipeline: 38 readable English
      cues in a sidecar VTT and embedded subtitle track, two-pass audio
      normalisation measured at about -18 LUFS / -1.8 dBTP, and a higher-quality
      H.264 encode. The resulting accessible draft is still not the final take.
- [x] Re-record the production walkthrough after the licensing state was
      resolved: `fortyguard-demo-jury-final.mp4`, 1440×810, 2:32.88, embedded
      English subtitles, -17.9 LUFS / -1.8 dBTP. Scenario Lab stays on K=10 and
      the report shows `Submission licensing: Ready`.
- [x] **Owner authorises the final publication.** Explicit `GO FINAL` received
      on 2026-08-26 after the final video and submission scope were presented.
- [x] Upload completed on the owner's instruction: <https://youtu.be/LPq0Tn6YX9w>,
      non-listed, 1:25, English captions active and no copyright issue reported
      by YouTube.
- [x] Re-recorded against deployed commit `e6240dbf` with the concise jury
      narrative and current full-map interface before submission.

## 4. Local — the text

- [x] [`submission-draft.md`](submission-draft.md) read once more against the
      claim table in `hackathon-compliance.md` §6.
- [x] Adapt the brief summary to the now-known form fields listed in §5. Do not
      paste the long draft verbatim.
- [x] README's *Pre-existing work* section still accurate for the commit being
      submitted.

## 5. External — nothing below can be done from this repository

- [x] **Push the final verified commit to the clean replacement repository**, and
      confirm in a logged-out browser that what is public is what was verified.
- [x] **Confirm the live demo** loads from a logged-out session and serves the
      pilot snapshot.
- [x] **Re-read the official hackathon page and current form on 2026-08-24** (§2
      and §3 of
      `hackathon-compliance.md`). It has been revised once already; nothing here
      detects a second revision.
- [x] **Add the collaborator recovered from the official form:** GitHub user
      `Hackathon-FG`, associated with `hackathon@fortyguard.com`, accepted
      `read` access on the clean repository. The GitHub permissions API was
      re-checked on 2026-08-24 and reports role `read`, with no pending invite.
- [x] **File the submission form**, distributed via Slack. Google Forms displayed
      “Your project is submitted” on 2026-08-26 at 18:51 Europe/Berlin.
- [x] **Submit by the owner-set target, 26 August.** Official deadline remains
      30 August at 23:59 GST; the confirmed submission is early.

### Submission-form field map — submitted 2026-08-26

The Google Form has six pages and states that the latest resubmission before the
deadline replaces earlier entries. Item 0 is resolved; the owner may submit after
the remaining personal review and declarations.

1. Registration email; `Solo` or `Team`.
2. Solo: full name and registered email. Team: team name, leader name and
   registered email, member count (1–3), and every member's name and email.
3. Project title; one-line pitch (**140 characters maximum**); project
   description; one primary track; up to two secondary tracks; audience/decision
   changed; geography and data period; brief FortyGuard API usage; FortyGuard API
   key; AI tools and their uses.
4. Repository URL; confirmation that `Hackathon-FG`
   (`hackathon@fortyguard.com`) is a collaborator; public live-demo URL that
   opens without login/install; YouTube or Loom video, **three minutes maximum**.
5. Declarations: repository created after kickoff; no committed/browser-visible
   API key; original work and registered team members accepting the terms.
6. Optional final note.

Recommended routing for FortyGuard: primary **Track 1 — Resilient Cities &
Infrastructure**; secondary **Track 7 — Data Analysis & Correlation**.

**Evidence recorded 2026-08-23:** the clean public submission repository was
created on 2026-08-23, after the 2026-08-18 onboarding/kickoff. The retained
2026-08-03 organiser email also permits building before the sprint. The required
form checkbox was personally authorised by the owner's explicit `GO FINAL`
instruction and accompanied by the timing disclosure in the optional note. The
FortyGuard API key remains absent from the repository and application code.

## 6. After submitting

- [x] Record what was actually submitted, and when, in
      `hackathon-compliance.md` §6 — replacing the intent with the fact.
- [x] Leave [`post-submission-architecture.md`](post-submission-architecture.md)
      unimplemented. It remains deliberately out of scope for this submission.

---

## What is deliberately *not* on this list

- Any change under `app/`, `components/`, `lib/`, `tests/`, `scripts/`, `data/`
  or `package*` in the name of "polish". The engine is verified; a late
  unverified edit is a worse risk than an imperfect surface.
- Rewriting dates, authors or messages to make the timeline look different. The
  only authorised rewrite was the path-only removal of two non-redistributable
  payloads; the pre-existing work remains disclosed.
- Any claim the registry blocks, added because it would demo well.
