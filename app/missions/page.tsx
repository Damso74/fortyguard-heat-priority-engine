import { MissionsBoard } from '@/components/operations/MissionsBoard'

export const metadata = { title: 'Inspection Missions', description: 'Turn the frozen priority plan into bounded, reviewable field inspections.' }

export default function MissionsPage() {
  return <MissionsBoard />
}
